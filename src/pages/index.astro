---
import BaseLayout from '../layouts/BaseLayout.astro';
import StatsCards from '../components/StatsCards.astro';
import ResearchList from '../components/ResearchList.astro';
import MeetingsPreview from '../components/MeetingsPreview.astro';
import ResearchMap from '../components/ResearchMap.astro';
import ActivityTimeline from '../components/ActivityTimeline.astro';
import Search from '../components/Search.astro';
---

<BaseLayout title="Home">
  <!-- Hero -->
  <header class="hero">
    <div class="badge">ADHD COACHING PROJECT</div>
    <h1>
      <span class="text-gradient">ADHDコーチング事業</span><br />
      リサーチ & プロジェクトハブ
    </h1>
    <p class="hero-desc">
      ADHDの特性理解からコーチングメソッド設計まで、学術リサーチ・議事録・参照資料を体系的に管理するナレッジベース。
    </p>
  </header>

  <!-- Section 1: Stats -->
  <StatsCards />

  <!-- Section 2: Research Articles (1-click access) -->
  <ResearchList />

  <!-- Section 3: Meetings Preview -->
  <MeetingsPreview />

  <!-- Section 4: Learning Roadmap -->
  <section class="roadmap-section">
    <div class="section-eyebrow" style="color: var(--purple);">Learning Roadmap</div>
    <h2 class="roadmap-title">学習ロードマップ</h2>
    <p class="roadmap-desc">テーマ間の依存関係と推奨学習順序。各テーマから関連リサーチ・議事録を参照できます。</p>
    <ResearchMap />
  </section>

  <!-- Section 5: Recent Activity -->
  <ActivityTimeline />

  <!-- Section 6: Search -->
  <Search />
</BaseLayout>

<style>
  .hero {
    text-align: center;
    padding: 40px 0 64px;
  }

  .hero h1 {
    font-size: 36px;
    margin: 16px 0;
    line-height: 1.4;
  }

  .hero-desc {
    color: var(--text-dim);
    font-size: 15px;
    max-width: 560px;
    margin: 0 auto;
  }

  .section-eyebrow {
    font-size: 11px;
    font-weight: 700;
    letter-spacing: 3px;
    text-transform: uppercase;
    margin-bottom: 8px;
  }

  .roadmap-section {
    margin-bottom: 64px;
  }

  .roadmap-title {
    margin-top: 0;
    margin-bottom: 8px;
  }

  .roadmap-desc {
    color: var(--text-dim);
    font-size: 14px;
    margin-bottom: 20px;
  }

  @media (max-width: 600px) {
    .hero h1 {
      font-size: 24px;
    }
  }
</style>
